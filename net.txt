输入图像  I (H×W×3)
   |
   v
┌─────────────────────────────────────────────────────────────┐
│ Encoder：Backbone（ResNet50 / MobileNetV2）                  │
│  - 提取多层特征                                              │
│  - 输出两份关键特征：                                        │
│     1) 低层特征 low_level (≈ H/4 × W/4, 边缘细节强)          │
│     2) 高层特征 high_level (≈ H/16 × W/16, 语义强)           │
└─────────────────────────────────────────────────────────────┘
            |                           |
            | high_level                | low_level
            v                           v
┌─────────────────────────────────────────────────────────────┐
│ ASPP（空洞空间金字塔池化）                                   │
│  分支1：1×1 Conv                                             │
│  分支2：3×3 Atrous Conv (rate=r1)                             │
│  分支3：3×3 Atrous Conv (rate=r2)                             │
│  分支4：3×3 Atrous Conv (rate=r3)                             │
│  分支5：Global Avg Pool → 1×1 Conv → 上采样                   │
│  → Concat → 1×1 Conv 融合                                    │
└─────────────────────────────────────────────────────────────┘
            |
            v
      ASPP输出  F_aspp (≈ H/16 × W/16)
            |
            v  上采样到 H/4 × W/4
┌─────────────────────────────────────────────────────────────┐
│ Decoder（轻量解码器，细化边界）                               │
│  low_level 先 1×1 Conv 降维（比如降到 48 通道）               │
│  concat( 上采样后的 F_aspp , 处理后的 low_level )             │
│  → 3×3 Conv + 3×3 Conv（可分离卷积也行）                      │
└─────────────────────────────────────────────────────────────┘
            |
            v
     得到特征 F_dec (≈ H/4 × W/4)
            |
            v  1×1 Conv 输出类别通道数 C
     logits (≈ H/4 × W/4 × C)
            |
            v  上采样到原图大小
输出分割结果  P (H×W×C) → argmax 得到 mask (H×W)
